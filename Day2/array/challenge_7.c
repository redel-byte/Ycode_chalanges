#include <stdio.h>

int main()
{
    int index;
    printf("Enter the value of the index: ");
    scanf("%d", &index);
    int arr_copp[index];
    int compare[index];
    int arr_input[index];
    int min;
    int index;
    int i;
    compare[index] = arr_input[index];
    while (i < index)
    {
        printf("arr[%d] = ", i);
        scanf("%d", arr_input[i]);
        i++;
    }
    //-----------------------------
    for (int b = 0; i < index; b++)
    {
        min = arr_input[0];
        if (min > arr_input[b])
        {
            min = arr_input[b];
            for (int b = 0; i <)
        }
    }
}